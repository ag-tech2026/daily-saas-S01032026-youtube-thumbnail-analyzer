---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/auth/google-sign-in-button.tsx
  - src/components/auth/sign-in-button.tsx
  - src/components/auth/sign-up-form.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/components/auth/user-profile.tsx
  - src/app/dashboard/page.tsx
autonomous: false
must_haves:
  truths:
    - "User sees a 'Continue with Google' button on both login and register pages"
    - "Google button triggers BetterAuth social sign-in flow with redirect to /dashboard"
    - "User can see their credit balance in the header user dropdown"
    - "Dashboard page displays the user credit balance"
    - "Google sign-in button has proper Google branding (Google logo SVG)"
    - "Login and register pages have visual separator ('or') between Google button and email form"
  artifacts:
    - path: "src/components/auth/google-sign-in-button.tsx"
      provides: "Reusable Google OAuth sign-in button component"
      min_lines: 20
    - path: "src/components/auth/user-profile.tsx"
      provides: "User profile dropdown with credit balance display"
      contains: "credits"
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard page with credit balance card"
      contains: "credits"
  key_links:
    - from: "src/components/auth/google-sign-in-button.tsx"
      to: "src/lib/auth-client.ts"
      via: "imports signIn and calls signIn.social({ provider: 'google' })"
      pattern: "signIn\\.social"
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/auth/google-sign-in-button.tsx"
      via: "imports and renders GoogleSignInButton component"
      pattern: "GoogleSignInButton"
    - from: "src/components/auth/user-profile.tsx"
      to: "session.user.credits"
      via: "reads credits from useSession hook"
      pattern: "credits"
---

<objective>
Create the Google sign-in button component, integrate it into login/register pages, and display credit balance in the header dropdown and dashboard.

Purpose: This plan delivers all user-facing UI for Phase 1 -- Google OAuth button for sign-in, and credit balance visibility. Without this, users cannot use Google login or see their credits.
Output: Google sign-in button on auth pages, credit balance in UserProfile dropdown, credit balance on dashboard.
</objective>

<execution_context>
@/home/ars/.claude/get-shit-done/workflows/execute-plan.md
@/home/ars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@src/components/auth/sign-in-button.tsx
@src/components/auth/sign-up-form.tsx
@src/components/auth/user-profile.tsx
@src/app/(auth)/login/page.tsx
@src/app/(auth)/register/page.tsx
@src/app/dashboard/page.tsx
@src/lib/auth-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GoogleSignInButton and add to login/register pages</name>
  <files>src/components/auth/google-sign-in-button.tsx, src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx</files>
  <action>
1. Create `src/components/auth/google-sign-in-button.tsx`:
   - "use client" directive
   - Import `Button` from `@/components/ui/button` and `signIn` from `@/lib/auth-client`
   - Add loading state with `useState` for the button (set true on click, disable button while loading)
   - On click, call `await signIn.social({ provider: "google", callbackURL: "/dashboard" })`
   - Wrap in try/catch -- if error, log to console (OAuth errors are usually shown by the provider)
   - Render a full-width outline Button with Google's colored logo SVG (4-color G logo as inline SVG paths -- use the exact SVG from the research file which has 4 path elements with the correct Google brand colors: blue #4285F4, green #34A853, yellow #FBBC05, red #EA4335)
   - Text: "Continue with Google"
   - Support both light and dark modes (use `currentColor` for the SVG paths is NOT correct for Google branding -- use the actual Google brand colors)

2. Update `src/app/(auth)/login/page.tsx`:
   - Import `GoogleSignInButton` from `@/components/auth/google-sign-in-button`
   - Import `Separator` from `@/components/ui/separator`
   - Add GoogleSignInButton ABOVE the existing SignInButton form inside CardContent
   - Add an "or" divider between them using a relative div with Separator and a centered "or" text span (see research code example for exact pattern: relative div with Separator, absolute positioned span with bg-background px-2 text)
   - Keep existing SignInButton and all its functionality unchanged

3. Update `src/app/(auth)/register/page.tsx`:
   - Same pattern as login: import GoogleSignInButton and Separator
   - Add GoogleSignInButton ABOVE the existing SignUpForm inside CardContent
   - Add the same "or" divider between Google button and email form
   - Keep existing SignUpForm unchanged

Important: The login page is a Server Component that renders client components. The GoogleSignInButton is a client component, so it can be rendered inside the Server Component page without issue.
  </action>
  <verify>
- `pnpm run typecheck` passes
- `pnpm run lint` passes
- `src/components/auth/google-sign-in-button.tsx` exists and exports `GoogleSignInButton`
- Login page imports and renders GoogleSignInButton
- Register page imports and renders GoogleSignInButton
  </verify>
  <done>Google sign-in button component exists with proper branding, appears on both login and register pages with "or" separator between it and the email/password form.</done>
</task>

<task type="auto">
  <name>Task 2: Display credit balance in UserProfile dropdown and dashboard</name>
  <files>src/components/auth/user-profile.tsx, src/app/dashboard/page.tsx</files>
  <action>
1. Update `src/components/auth/user-profile.tsx`:
   - Add `Coins` to the lucide-react import (alongside existing User, LogOut)
   - After the DropdownMenuLabel (user name/email) and its DropdownMenuSeparator, add a credits display section:
     ```
     <DropdownMenuSeparator />
     <div className="px-2 py-1.5">
       <div className="flex items-center justify-between text-sm">
         <div className="flex items-center gap-2 text-muted-foreground">
           <Coins className="h-4 w-4" />
           <span>Credits</span>
         </div>
         <span className="font-semibold tabular-nums">
           {(session.user as any)?.credits ?? 0}
         </span>
       </div>
     </div>
     ```
   - Note: Use `(session.user as any)?.credits` if the TypeScript types don't include credits yet. If Plan 01's session extension worked and types flow through, use `session.user.credits` directly. The executor should check what typecheck says and adjust accordingly.
   - Place this credits section between the user info label and the "Your Profile" menu item
   - Keep all existing functionality (avatar, profile link, sign out) unchanged

2. Update `src/app/dashboard/page.tsx`:
   - Add `Coins` to the lucide-react import
   - In the authenticated dashboard view (inside the `return` after the session check), add a credit balance card to the existing grid:
     ```
     <div className="p-6 border border-border rounded-lg">
       <h2 className="text-xl font-semibold mb-2">Credits</h2>
       <p className="text-muted-foreground mb-4">
         Your analysis credit balance
       </p>
       <div className="flex items-center gap-3">
         <Coins className="h-8 w-8 text-primary" />
         <span className="text-4xl font-bold tabular-nums">
           {(session.user as any)?.credits ?? 0}
         </span>
       </div>
     </div>
     ```
   - Add this as a third card in the grid (after AI Chat and Profile cards)
   - Update the grid to handle 3 columns on large screens: change `md:grid-cols-2` to `md:grid-cols-2 lg:grid-cols-3`
   - Same note about TypeScript types as above -- use `as any` cast if needed

Important: The `tabular-nums` class ensures credit numbers don't shift layout when they change (e.g., 3 vs 50). This is a small but important UX detail.
  </action>
  <verify>
- `pnpm run typecheck` passes
- `pnpm run lint` passes
- UserProfile dropdown shows credits with Coins icon
- Dashboard has a credits card showing the balance
  </verify>
  <done>Credit balance is visible in both the header user dropdown (UserProfile component) and the dashboard page as a dedicated card with large number display.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Google OAuth flow and credit display</name>
  <files>no files modified -- verification only</files>
  <action>
Human verification of the complete Phase 1 implementation. No code changes -- this is a checkpoint for the user to verify:
- Google OAuth sign-in button on login and register pages
- Credit system with 3 free credits for new users
- Credit balance display in header dropdown and dashboard
- Session persistence configured for 7 days

**Prerequisites:** User needs Google OAuth credentials set up. Add to `.env`:
```
GOOGLE_CLIENT_ID=your_client_id
GOOGLE_CLIENT_SECRET=your_client_secret
BETTER_AUTH_URL=http://localhost:3000
```

If credentials don't exist yet:
1. Go to https://console.cloud.google.com/apis/credentials
2. Create a new OAuth 2.0 Client ID (Web application)
3. Add `http://localhost:3000/api/auth/callback/google` as authorized redirect URI
4. Copy Client ID and Client Secret to `.env`

**Testing steps:**

1. Start the dev server: `pnpm dev`

2. **Test Google OAuth (AUTH-02):**
   - Visit http://localhost:3000/login
   - Verify "Continue with Google" button above email form with "or" separator
   - Click Google button and complete sign-in flow
   - Verify redirect to /dashboard

3. **Test credit initialization (AUTH-03):**
   - After Google sign-in, check dashboard Credits card shows "3"
   - Click avatar in header, verify dropdown shows "Credits: 3"

4. **Test email signup credits:**
   - Sign out, register new account with email/password at /register
   - Verify register page also has Google button
   - After registration, check dashboard shows "3" credits

5. **Test session persistence (AUTH-04):**
   - Close browser completely (not just tab)
   - Reopen and visit http://localhost:3000/dashboard
   - Verify still logged in

6. **Test existing auth still works (AUTH-01):**
   - Sign out, sign in with email/password at /login
   - Verify dashboard loads with credits visible
  </action>
  <verify>All 6 testing steps pass as described above.</verify>
  <done>User has confirmed: Google OAuth works end-to-end, credits initialize to 3 on signup, credit balance displays in header dropdown and dashboard, session persists across browser restarts, existing email/password auth still works.</done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` passes
2. `pnpm run lint` passes
3. GoogleSignInButton component exists and is used on both login and register pages
4. UserProfile dropdown displays credit balance with Coins icon
5. Dashboard page has credits card with large balance display
6. Google OAuth flow redirects to /dashboard on success
7. New users see 3 credits after signup
</verification>

<success_criteria>
- "Continue with Google" button appears on both /login and /register pages
- Google OAuth triggers BetterAuth social sign-in and redirects to /dashboard
- Header user dropdown shows credit balance (Coins icon + number)
- Dashboard displays credit balance in a dedicated card
- Visual separator ("or") divides Google button from email/password form
- All existing auth functionality (email/password login, register, sign out) still works
- TypeScript and ESLint pass with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
