---
phase: 07-landing-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/components/landing/hero-section.tsx
  - src/components/landing/analysis-mockup.tsx
  - src/components/landing/pricing-section.tsx
  - src/components/landing/landing-cta-section.tsx
  - src/components/landing/sign-up-cta-button.tsx
autonomous: true

must_haves:
  truths:
    - "Landing page hero section clearly states value proposition with outcome-oriented headline"
    - "Landing page shows a static mockup of the analysis result UI on the right side of the hero"
    - "Landing page displays pricing with two tiers: free (3 analyses) and pro ($9 for 50 analyses)"
    - "Landing page has prominent CTA button that triggers Google OAuth signup"
    - "Page flow is Hero -> Pricing -> CTA (no demo section)"
    - "CTA uses 'Analyze Your First 3 Hands Free' language, not 'credits'"
    - "Page metadata shows poker-specific title and description, not boilerplate"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page Server Component assembling all sections"
      contains: "export const metadata"
    - path: "src/components/landing/hero-section.tsx"
      provides: "Split layout hero with headline left, mockup right"
      min_lines: 20
    - path: "src/components/landing/analysis-mockup.tsx"
      provides: "Static replica of Phase 6 analysis UI with hardcoded data"
      min_lines: 40
    - path: "src/components/landing/pricing-section.tsx"
      provides: "Two-tier pricing cards (free + pro)"
      min_lines: 30
    - path: "src/components/landing/landing-cta-section.tsx"
      provides: "Bottom CTA strip with signup button"
      min_lines: 10
    - path: "src/components/landing/sign-up-cta-button.tsx"
      provides: "Client component wrapping GoogleSignInButton for landing page"
      contains: "use client"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/landing/hero-section.tsx"
      via: "import HeroSection"
      pattern: "import.*HeroSection"
    - from: "src/app/page.tsx"
      to: "src/components/landing/pricing-section.tsx"
      via: "import PricingSection"
      pattern: "import.*PricingSection"
    - from: "src/app/page.tsx"
      to: "src/components/landing/landing-cta-section.tsx"
      via: "import LandingCtaSection"
      pattern: "import.*LandingCtaSection"
    - from: "src/components/landing/hero-section.tsx"
      to: "src/components/landing/analysis-mockup.tsx"
      via: "import AnalysisMockup"
      pattern: "import.*AnalysisMockup"
    - from: "src/components/landing/sign-up-cta-button.tsx"
      to: "src/lib/auth-client"
      via: "signIn.social for Google OAuth"
      pattern: "signIn\\.social"
---

<objective>
Replace the boilerplate homepage with a product-specific marketing landing page for the poker hand analysis tool.

Purpose: Convert visitors into signups by clearly communicating the product's value proposition (upload a poker screenshot, get GTO analysis), showing realistic UI output, displaying pricing, and providing a prominent signup CTA.

Output: A complete public landing page with hero section (split layout: headline + CTA left, analysis mockup right), two-tier pricing section, and bottom CTA. All boilerplate content removed. Page metadata updated for SEO.
</objective>

<execution_context>
@/home/ars/.claude/get-shit-done/workflows/execute-plan.md
@/home/ars/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-landing-page-polish/07-CONTEXT.md
@.planning/phases/07-landing-page-polish/07-RESEARCH.md
@src/app/page.tsx
@src/app/layout.tsx
@src/app/analysis/[id]/page.tsx
@src/lib/analysis-schema.ts
@src/components/auth/google-sign-in-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page components</name>
  <files>
    src/components/landing/sign-up-cta-button.tsx
    src/components/landing/analysis-mockup.tsx
    src/components/landing/hero-section.tsx
    src/components/landing/pricing-section.tsx
    src/components/landing/landing-cta-section.tsx
  </files>
  <action>
Create `src/components/landing/` directory and 5 component files:

**sign-up-cta-button.tsx** — "use client" component. Wraps the Google OAuth sign-in flow. Import `signIn` from `@/lib/auth-client`. On click, call `signIn.social({ provider: "google", callbackURL: "/dashboard" })`. Accept `children` prop for custom button text and optional `className`, `size`, `variant` props (pass through to shadcn Button). Default text: "Analyze Your First 3 Hands Free". Show "Connecting..." while loading. Include Google logo SVG inline (same as existing GoogleSignInButton pattern). This is a NEW component — do NOT modify the existing GoogleSignInButton.

**analysis-mockup.tsx** — Server Component (NO "use client"). Import `AnalysisResult` type from `@/lib/analysis-schema`. Define a `DEMO_ANALYSIS` constant with hardcoded realistic data matching the schema exactly (use the fixture from RESEARCH.md: AKo on BTN, $0.10/$0.25, flop K7d2s, turn Qc). Render a SCALED-DOWN version of the analysis result UI that visually matches `src/app/analysis/[id]/page.tsx`. Use the same Card, Badge, CardHeader, CardTitle, CardContent components from shadcn/ui. Show these sections in a compact card layout:
  - Hand Details header (stakes, position, hand)
  - Good Plays section (show 2-3 items with checkmark styling)
  - Areas to Improve section (show 1-2 items)
  - Bottom Line callout

Apply a visual treatment to make it look like a product preview: add `rounded-xl border shadow-lg overflow-hidden` wrapper, use `scale-90` or smaller text sizes to make it feel like a miniaturized app view. Do NOT add rotation or skew transforms (keep it readable). Apply `pointer-events-none select-none` so it's clearly non-interactive.

**hero-section.tsx** — Server Component. Split layout using Tailwind grid: `grid grid-cols-1 lg:grid-cols-2 gap-12 items-center`. Left side contains:
  - H1 headline: outcome-oriented, speaks to the pain (per locked decision). Example: "Find Out If Your Poker Plays Are Actually Correct". Use `text-4xl lg:text-5xl font-bold tracking-tight`.
  - Subtitle: "Upload a screenshot of any poker hand. Get instant GTO analysis that tells you exactly what you did right and wrong." Use `text-xl text-muted-foreground`.
  - SignUpCtaButton with default text "Analyze Your First 3 Hands Free" (per locked decision). Make it large: `size="lg"`.
  - Below button, small trust text: "No credit card required. 3 free analyses on signup." in `text-sm text-muted-foreground`.
Right side: render `<AnalysisMockup />`.
Wrap in `<section className="container mx-auto px-4 py-16 lg:py-24">`.

**pricing-section.tsx** — Server Component. Two-tier pricing using shadcn Card components side by side. Use `grid grid-cols-1 md:grid-cols-2 gap-6 max-w-2xl mx-auto`.

Free tier card:
  - CardTitle: "Free"
  - Price: "$0" in large bold text
  - Features list: "3 hand analyses", "Full GTO feedback", "Beginner-friendly explanations"
  - Footer: SignUpCtaButton with text "Get Started Free" and `variant="outline"`

Pro Pack card (highlighted with `border-primary` and `relative overflow-hidden`):
  - Add a "Best Value" badge: `<Badge className="absolute top-4 right-4">Best Value</Badge>`
  - CardTitle: "Pro Pack"
  - Price: "$9" in large bold text, with "one-time" subtitle
  - Features list: "50 hand analyses", "Never expires", "Full GTO feedback", "$0.18 per analysis"
  - Footer: SignUpCtaButton with text "Sign Up to Purchase" (per research: do NOT use PurchaseCreditsButton on public page — requires auth. Use signup button instead; users can purchase from dashboard after signup.)

Section header: "Simple Pricing" centered above cards. Wrap in `<section className="container mx-auto px-4 py-16 bg-muted/30">` with a muted background band.

**landing-cta-section.tsx** — Server Component. Bottom CTA strip. Centered text: "Ready to Improve Your Poker Game?" in `text-3xl font-bold`. Subtitle: "Join players who analyze their hands with AI-powered GTO feedback." SignUpCtaButton with default text. Wrap in `<section className="container mx-auto px-4 py-16 text-center">`.
  </action>
  <verify>
All 5 files exist in `src/components/landing/`. Run `pnpm lint && pnpm typecheck` — both pass with no errors in the landing components.
  </verify>
  <done>
Five landing page components created: sign-up-cta-button (client), analysis-mockup (server, uses AnalysisResult type with hardcoded data), hero-section (split layout with headline + mockup), pricing-section (two-tier cards), landing-cta-section (bottom CTA). All use "analyses" language, not "credits". CTA button text is "Analyze Your First 3 Hands Free" per locked decision.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace homepage and update metadata</name>
  <files>
    src/app/page.tsx
    src/app/layout.tsx
  </files>
  <action>
**Replace src/app/page.tsx entirely.** Delete ALL existing content (boilerplate "use client", useDiagnostics hook, SetupChecklist, StarterPromptModal, YouTube iframe, feature grid). Start fresh as a Server Component (NO "use client").

New page.tsx content:
1. Export `metadata` const (type `Metadata` from `next`) with:
   - title: "Poker Hand Analyzer — Instant GTO Analysis from Screenshots"
   - description: "Upload a poker screenshot and get instant GTO analysis. Find leaks in your game in 30 seconds. 3 free analyses on signup."
   - openGraph with same title and description
2. Default export `HomePage` function that returns:
   ```
   <main>
     <HeroSection />
     <PricingSection />
     <LandingCtaSection />
   </main>
   ```
   Import all three from `@/components/landing/`. Page flow is Hero -> Pricing -> CTA (no demo section — per locked decision).

**Update src/app/layout.tsx metadata:**
1. Change `title.default` from "Agentic Coding Boilerplate" to "Poker Hand Analyzer"
2. Change `title.template` to "%s | Poker Hand Analyzer"
3. Update `description` to poker-specific copy: "Upload poker screenshots and get instant GTO analysis. AI-powered hand review for all skill levels."
4. Update `keywords` array to poker-relevant terms: ["poker", "GTO", "hand analysis", "poker screenshot", "game theory optimal", "poker AI", "hand review"]
5. Update `openGraph` title, description, and siteName to match
6. Update `twitter` title and description to match
7. Remove `authors` and `creator` fields (boilerplate attribution)
8. Update JSON-LD structured data: change name, description, applicationCategory to "GameApplication"

Note: The `<main id="main-content">` wrapper is already in layout.tsx, so page.tsx should use a plain `<div>` or fragment instead of `<main>` to avoid nested main elements. Use `<div className="flex-1">` or just `<>...</>` in page.tsx.
  </action>
  <verify>
Run `pnpm lint && pnpm typecheck` — both pass. Verify page.tsx does NOT contain "use client", useDiagnostics, SetupChecklist, or StarterPromptModal. Verify page.tsx exports metadata with poker-specific title. Verify layout.tsx metadata no longer references "Boilerplate" or "Leon van Zyl".
  </verify>
  <done>
Homepage fully replaced with landing page Server Component. Page assembles Hero -> Pricing -> CTA (no demo). Metadata updated across page.tsx (page-level) and layout.tsx (site-wide) with poker-specific title, description, keywords, OG tags, and JSON-LD. All boilerplate content removed.
  </done>
</task>

</tasks>

<verification>
1. `pnpm lint` passes with no errors
2. `pnpm typecheck` passes with no errors
3. `src/app/page.tsx` is a Server Component (no "use client")
4. `src/app/page.tsx` exports metadata with poker-specific title
5. Page flow: HeroSection -> PricingSection -> LandingCtaSection (no demo)
6. Hero has split layout: headline left, analysis mockup right
7. CTA button text: "Analyze Your First 3 Hands Free"
8. Pricing shows two tiers: Free ($0, 3 analyses) and Pro ($9, 50 analyses)
9. No boilerplate content remains (SetupChecklist, StarterPromptModal, YouTube iframe removed)
10. Layout metadata updated — no references to "Boilerplate"
</verification>

<success_criteria>
- Landing page builds and type-checks without errors
- Page renders three sections: hero (split layout), pricing (two cards), bottom CTA
- Hero shows outcome-oriented headline and analysis mockup preview
- All CTAs trigger Google OAuth signup flow
- Pricing uses "analyses" language, not "credits"
- Metadata is poker-specific throughout (page + layout)
</success_criteria>

<output>
After completion, create `.planning/phases/07-landing-page-polish/07-01-SUMMARY.md`
</output>
